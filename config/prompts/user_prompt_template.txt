用户偏好：
{user_info}

新活动信息：
{activity_info}


请判断用户是否对这个活动感兴趣，返回 JSON 格式。
注意！如果活动没有明显不符合用户偏好的地方，你必须认为用户对这个活动感兴趣。
如果活动信息既符合用户对自己“感兴趣”活动的描述，又符合用户对“不感兴趣”活动的描述，你应当认为用户对这个活动感兴趣。    # 网络错误（连接超时、断开等）是否重试
只返回 JSON，不要返回其他内容。
